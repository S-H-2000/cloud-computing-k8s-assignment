apiVersion: v1
kind: Service
metadata:
  name: pet-store2
  namespace: pet-business
spec:
  selector:
    app: pet-store2
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
